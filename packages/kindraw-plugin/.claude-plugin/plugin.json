{
  "name": "kindraw",
  "displayName": "Kindraw",
  "version": "0.1.0",
  "description": "Let Claude create real, shareable drawings and diagrams in your Kindraw workspace instead of ASCII art.",
  "author": {
    "name": "Matheus Kindrazki",
    "url": "https://github.com/MatheusKindrazki/kindraw"
  },
  "homepage": "https://kindraw.dev",
  "repository": "https://github.com/MatheusKindrazki/kindraw",
  "license": "MIT",
  "keywords": ["drawing", "diagram", "mermaid", "excalidraw", "kindraw"],
  "mcpServers": {
    "kindraw": {
      "command": "npx",
      "args": ["-y", "@kindraw/mcp"],
      "env": {
        "KINDRAW_TOKEN": "${user_config.api_token}",
        "KINDRAW_API_BASE_URL": "${user_config.api_base_url}"
      }
    }
  },
  "userConfig": {
    "api_token": {
      "type": "string",
      "title": "Kindraw API token",
      "description": "Generate one at https://kindraw.dev → sign in with GitHub → profile → API tokens. The token has full access to your workspace.",
      "sensitive": true,
      "required": true
    },
    "api_base_url": {
      "type": "string",
      "title": "API base URL (advanced)",
      "description": "Leave as default unless you self-host Kindraw.",
      "default": "https://api.kindraw.dev",
      "required": false
    }
  },
  "defaultEnabled": false
}
